"""7-stage analysis pipeline (HLD §5). Implemented by another agent.

`app/worker.py`'s `analyze` job currently uses a stub; once the real
pipeline stages land here, the worker job should call into this package
without changing the job signature or the `analyses` row contract.
"""
